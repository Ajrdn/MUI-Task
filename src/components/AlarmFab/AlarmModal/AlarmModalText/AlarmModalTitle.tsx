import { styled } from '@mui/material/styles'
import Typography from '@mui/material/Typography'


const AlarmModalTitle = styled(Typography)({
  fontSize: '26px',
  textAlign: 'center',
  fontFamily: 'Pretendard',
  fontWeight: 600,
})

export default AlarmModalTitle
