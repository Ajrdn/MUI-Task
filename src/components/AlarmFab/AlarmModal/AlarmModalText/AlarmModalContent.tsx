import { styled } from '@mui/material/styles'
import Typography from '@mui/material/Typography'


const AlarmModalContent = styled(Typography)({
  fontSize: '18px',
  textAlign: 'center',
  fontFamily: 'Pretendard',
  fontWeight: 400,
  whiteSpace: 'normal',
})

export default AlarmModalContent
